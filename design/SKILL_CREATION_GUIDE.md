# Claude Code Skill 创建指南

> 基于 skill-best-practice.txt 提炼的系统性方法论，帮助开发者创建高质量的 Claude Code Skills

## 🎯 目录

- [一、适用性判断](#一适用性判断)
- [二、核心设计原则](#二核心设计原则)
- [三、抽象与拆分策略](#三抽象与拆分策略)
- [四、颗粒度控制](#四颗粒度控制)
- [五、工程化实践](#五工程化实践)
- [六、评估清单](#六评估清单)
- [七、案例分析](#七案例分析)
- [八、模板和工具](#八模板和工具)

---

## 一、适用性判断

### ✅ 适合开发为 Skill 的任务特征

| 特征 | 描述 | 示例 |
|------|------|------|
| **重复性高** | 任务在多个用户请求中反复出现 | "生成 PPT"、"提取 PDF 表格"、"部署智能合约" |
| **流程固定** | 具有清晰、可标准化的操作步骤 | "解包 → 修改 XML → 打包"、"编译 → 测试 → 部署" |
| **工具链复杂** | 涉及多工具协作且易出错 | CLI + Python + JS + 模板 + 区块链工具 |
| **输出结构化** | 产出为文件或结构化数据 | .pptx/.pdf/.html 文件、JSON 配置、ABI 文件 |
| **需一致性保障** | 要求品牌、格式、安全等一致性 | 企业 UI 规范、Hardhat 安全标准、代码风格 |
| **需交互探索** | 支持参数调整、种子导航、多变体生成 | 算法艺术、模板定制、网络配置选择 |

### ❌ 不适合开发为 Skill 的任务

- 一次性、高度定制化、无通用模式的请求
- 纯自然语言问答（如"解释以太坊"）
- 无明确输入/输出边界的开放式创作

### 🎯 决策点

**若某类任务满足 ≥3 项"适合"特征，应考虑封装为 Skill。**

---

## 二、核心设计原则

### 1. 面向意图，而非操作

```yaml
# ❌ 错误示例 - 面向操作
name: pdf-processor
description: 处理PDF文件

# ✅ 正确示例 - 面向意图
name: contract-deployer
description: 一键式智能合约部署工具，支持多网络部署和自动验证
```

**核心要点:**
- Skill 应响应用户意图（"我要创建带图表的 PPT"）
- 内部将意图映射到标准化工作流（Workflow）
- 提供业务语义而非技术细节

### 2. 端到端闭环

```
输入（文件/参数） → 处理 → 验证 → 输出（文件/报告）
                    ↓
              错误反馈 → 修复建议 → 重试
```

**实施要点:**
- 从输入到输出全程自动化
- 包含验证、错误反馈、修复建议
- 提供清晰的成功/失败状态

### 3. 可组合与可扩展

```
inventory.py ← extract_text_inventory()
                 ↓
rearrange.py ← reorder_slides()
                 ↓
replace.py ← replace_content()
```

**设计原则:**
- 模块化脚本可被多个 Skill 复用
- 支持模板系统和配置驱动
- 避免重复代码和功能

### 4. 用户体验优先

```python
# 提供预览功能
def generate_thumbnail(output_path):
    """生成输出文件的预览缩略图"""

# 支持交互控制
def interactive_mode():
    """提供参数滑块、种子导航等交互功能"""

# 输出自解释
def generate_manifest():
    """生成清单文件和验证报告"""
```

---

## 三、抽象与拆分策略

### 1. 分层抽象模型

```
User Intent (用户意图)
    ↓
Skill Interface (技能接口：name + description)
    ↓
Workflow Orchestrator (工作流编排器：决策树/步骤序列)
    ↓
Atomic Tools (原子工具：unpack.py, extract_text, deploy_contracts.py)
    ↓
Low-level Libraries (底层库：pypdf, p5.js, Hardhat, markitdown)
```

### 2. 操作拆分颗粒度

| 层级 | 职责 | 示例 |
|------|------|------|
| **Skill 层** | 业务意图 | `pptx`: 创建/编辑 PPT |
| **Workflow 层** | 逻辑阶段 | "使用模板创建 PPT" → 5 步流程 |
| **Script 层** | 可复用操作 | `inventory.py`: 提取结构化文本 |
| **Function 层** | 原子功能 | `extract_text_inventory()` / `save_inventory()` |

### 3. 关键原则

- ✅ 每个 script 应单一职责
- ✅ workflow 负责编排，不实现细节
- ❌ 避免"上帝脚本"（一个脚本做所有事）

---

## 四、颗粒度控制

### ✅ 合理颗粒度的标准

1. **可独立测试**: 每个 script 可单独运行并验证
2. **可被组合**: `rearrange.py + inventory.py + replace.py = 模板化 PPT 生成`
3. **可被复用**: `thumbnail.py` 既用于模板分析，也用于最终验证
4. **错误隔离**: 某步失败不影响其他步骤

### ⚠️ 常见误区

| 误区 | 问题 | 解决方案 |
|------|------|----------|
| **过粗** | 一个脚本处理"整个 PPT 创建"，难以调试、复用 | 按功能边界拆分 |
| **过细** | 每个小操作都拆成脚本，增加调用复杂度 | 合并相关操作 |

### 💡 建议

**以用户可理解的操作单元为边界**（如"提取文本"、"重排幻灯片"、"替换内容"）。

---

## 五、工程化实践

### 1. 标准化输入/输出

```python
# 输入格式
def process_skill(input_file: str, params: dict, output_dir: str):
    """标准化输入接口"""
    pass

# 输出格式
def generate_output():
    """
    返回结构:
    {
        "success": bool,
        "files": [str],  # 生成的文件列表
        "logs": str,     # 详细日志
        "errors": str    # 错误信息（如有）
    }
    """
    pass

# 错误处理
def handle_error(error_type: str, context: dict):
    """
    示例错误信息:
    "shape-99 不存在，可用：shape-0,1,4"
    """
    pass
```

### 2. 强制文档与模板

```
skill-name/
├── SKILL.md              # 必须定义
├── scripts/              # 脚本目录
├── templates/            # 模板文件
├── references/           # 参考文档
├── assets/              # 静态资源
└── examples/            # 使用示例
```

### 3. 验证驱动开发

```python
def validate_output(output_path: str):
    """输出前验证"""
    # 视觉检查（缩略图）
    # 逻辑检查（结构验证）
    # 完整性检查（文件存在性）
    pass

def run_tests():
    """自动化测试套件"""
    # 单元测试
    # 集成测试
    # 端到端测试
    pass
```

### 4. 依赖管理

```python
# requirements.txt
pypdf>=3.0.0
pillow>=10.0.0
hardhat>=2.17.0

# setup.py 或一键安装脚本
def setup_environment():
    """自动安装依赖和环境配置"""
    pass
```

---

## 六、评估清单

### 📋 开发前评估

在决定开发新 Skill 前，问自己：

| 问题 | 评估标准 |
|------|----------|
| **输入输出映射** | 是否有清晰的输入 → 输出映射？ |
| **标准流程** | 是否存在可复现的标准流程？ |
| **封装价值** | 是否值得封装（使用频率 > 3 次）？ |
| **原子操作** | 是否能拆分为 3–5 个原子操作？ |
| **验证机制** | 是否有验证/回滚机制？ |
| **价值提升** | 是否提升一致性、安全性或效率？ |

**若多数为"是"，则值得开发。**

### 📊 质量评估矩阵

| 维度 | 优秀 | 良好 | 需改进 |
|------|------|------|--------|
| **抽象层级** | 意图 → 工作流 → 脚本 | 部分抽象 | 直接暴露底层命令 |
| **可复用性** | 多个流程共享脚本 | 部分复用 | 每个任务写新脚本 |
| **用户控制** | 参数+预览+导航 | 基本控制 | 黑盒输出 |
| **错误处理** | 明确报错+选项 | 基本错误处理 | 程序崩溃无提示 |
| **一致性** | 强制模板/UI/字体 | 部分一致性 | 风格随意 |

---

## 七、案例分析

### 🏆 优秀 Skill 示例：hardhat-manager

```yaml
name: hardhat-manager
description: 全面的 Hardhat 开发环境管理工具
```

**优秀特征:**
- ✅ **面向意图**: "管理 Hardhat 项目"而非"运行命令"
- ✅ **端到端**: 从环境设置到部署验证全流程
- ✅ **模块化**: 12个专用脚本，各司其职
- ✅ **可复用**: 脚本可组合使用
- ✅ **用户体验**: 交互式模式、进度反馈、错误处理

### 🔧 架构分析

```
User Intent: "我要创建一个 NFT 项目"
    ↓
Skill Interface: hardhat-manager
    ↓
Workflow: 项目模板化创建流程
    ├── setup_project.py (项目初始化)
    ├── contract_generator.py (合约生成)
    ├── deploy_contracts.py (部署脚本)
    └── verify_contracts.py (合约验证)
    ↓
Atomic Tools:
    ├── network_config.py (网络配置)
    ├── template_manager.py (模板管理)
    └── security_scanner.py (安全扫描)
    ↓
Libraries: Hardhat, OpenZeppelin, Ethers.js
```

### ❌ 需改进的反面示例

```yaml
# 问题示例
name: generic-helper
description: 帮助处理各种任务
```

**问题分析:**
- ❌ **意图不明确**: "各种任务"过于宽泛
- ❌ **无标准流程**: 缺乏可复现的操作序列
- ❌ **无边界**: 输入输出不清晰
- ❌ **无验证机制**: 缺乏错误处理和反馈

---

## 八、模板和工具

### 📁 标准目录结构

```
skill-name/
├── SKILL.md                    # 技能定义文件
├── scripts/                    # 核心脚本
│   ├── main.py                # 主入口脚本
│   ├── workflow.py            # 工作流编排
│   ├── utils/                 # 工具函数
│   └── validators.py          # 验证脚本
├── templates/                  # 模板文件
│   ├── config.template.json   # 配置模板
│   └── output.template.html   # 输出模板
├── references/                 # 参考文档
│   ├── api.md                 # API 文档
│   └── examples.md            # 使用示例
├── assets/                     # 静态资源
│   ├── icons/                 # 图标资源
│   └── samples/               # 示例文件
├── tests/                      # 测试文件
│   ├── unit/                  # 单元测试
│   └── integration/           # 集成测试
├── requirements.txt            # Python 依赖
├── setup.py                   # 安装脚本
└── README.md                  # 项目说明
```

### 🛠️ 开发工具箱

#### 1. Skill 生成器模板

```python
# scripts/skill_generator.py
def generate_skill_template(skill_name: str, description: str):
    """生成标准 Skill 目录结构"""
    pass

def create_skill_md(skill_name: str, description: str):
    """生成 SKILL.md 文件"""
    pass

def setup_basic_workflow():
    """创建基础工作流脚本"""
    pass
```

#### 2. 验证工具

```python
# scripts/validator.py
def validate_skill_structure(skill_path: str):
    """验证 Skill 目录结构完整性"""
    pass

def validate_skill_md(skill_md_path: str):
    """验证 SKILL.md 格式正确性"""
    pass

def run_skill_tests(skill_path: str):
    """运行 Skill 测试套件"""
    pass
```

#### 3. 文档生成器

```python
# scripts/doc_generator.py
def generate_api_docs(scripts_dir: str):
    """从脚本自动生成 API 文档"""
    pass

def generate_examples(skill_path: str):
    """生成使用示例文档"""
    pass
```

### 📋 SKILL.md 模板

```yaml
---
name: skill-name
description: 清晰描述技能用途和使用场景
license: Apache 2.0
allowed-tools: [Bash, Read, Write, Edit, Glob, Grep]
metadata:
  category: 区块链/文档处理/数据分析
  tags: [solidity, pdf, security, automation]
  version: 1.0.0
---

# Skill 名称

## 概述
简要描述技能的主要功能和用途

## 使用场景
这个技能应该在你需要：
- 具体场景1
- 具体场景2
- 具体场景3

时使用。

## 核心功能
- 功能1：描述
- 功能2：描述
- 功能3：描述

## 使用方法
### 基础用法
```bash
# 命令示例
python3 scripts/main.py --input <file> --output <dir>
```

### 高级用法
```bash
# 高级命令示例
python3 scripts/main.py --config <config.json> --interactive
```

## 输出格式
- **文件输出**: 生成的文件类型和格式
- **报告输出**: 结构化数据格式
- **日志输出**: 日志级别和格式

## 配置选项
| 参数 | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| --input | string | - | 输入文件路径 |
| --output | string | ./output | 输出目录 |
| --config | string | config.json | 配置文件 |

## 故障排除
### 常见问题
1. **问题1**: 解决方案
2. **问题2**: 解决方案

### 调试模式
```bash
# 启用详细日志
python3 scripts/main.py --debug --verbose
```

## 最佳实践
1. 使用建议1
2. 使用建议2
3. 注意事项
```

---

## 🎯 总结

开发一个高质量的 Claude Code Skill，本质是将复杂但重复的技术任务，转化为：

> **意图驱动、流程标准、体验一致、可验证可组合的自动化服务**

### 成功关键

> **"抽象恰到好处，封装恰如其分，用户恰得其所。"**

### 记住

1. **从用户意图出发，不要陷入技术细节**
2. **保持简单，但不要简化过度**
3. **每一次修改都要验证，每一次发布都要文档化**
4. **为复用而设计，为组合而思考**
5. **用户体验不是可选项，而是核心要求**

---

*本指南基于 contract-skills 项目的实践经验，持续更新中。欢迎贡献案例和改进建议！*